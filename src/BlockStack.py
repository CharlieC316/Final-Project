import pygame
import random

# Initialize pygame
pygame.init()

# Constants
SCREEN_WIDTH, SCREEN_HEIGHT = 300, 600
BLOCK_SIZE = 30
FPS = 60

# Colors & Textures
white = (255, 255, 255)
red = (255, 0, 0)
backgroundImage = pygame.image.load("BackgroundImage.jpg")

pygame.quit()